{
  "name": "@zk-intents/circuits",
  "version": "0.1.0",
  "description": "Zero-knowledge circuits for zk-Intents rollup",
  "scripts": {
    "build": "bash ../scripts/build_circuits.sh",
    "test": "mocha test/**/*.test.js",
    "clean": "rm -rf build *.r1cs *.wasm *.zkey *.vk.json pot*"
  },
  "devDependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.3",
    "circom_tester": "^0.0.19",
    "mocha": "^10.2.0",
    "chai": "^4.3.10"
  }
}
